# Architecture

> How the system is built. Updated when patterns change, not per session.
> Audit when this file passes 500 lines — extract topic pages and link from [[INDEX]].

## Stack

<!-- Languages, frameworks, key dependencies WITH versions. -->

_Not yet mapped._

## Layout

<!-- Where things live. Directory map with one line per area. -->

## Patterns

<!-- Conventions that code must follow: error handling, naming, state, testing.
     One bullet per pattern, concrete: "API errors → AppError subclass in src/errors/, never raw throw". -->

## Gotchas

<!-- Non-obvious traps that cost someone time. The most valuable section in this file.
     Format: "X looks like Y but actually Z — because <reason>". -->
